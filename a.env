PORT=3000
JWT_SECRET=troque_para_uma_chave_secreta_long
JWT_EXPIRES_IN=7d
DB_FILE=./data/powerbody.db

# nodemailer SMTP (para reset de senha). Configure com seu SMTP.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=seu_usuario
SMTP_PASS=sua_senha
FROM_EMAIL=nao-responda@seusite.com

# URL pública do site (para construir link de reset)
APP_URL=http://localhost:3000
